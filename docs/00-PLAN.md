# 러크 서버(Ruc Server) — 개발 계획서

> 작성: 2026-09-12 · 기준 문서: `ruc-server-project-prompt.md`

---

## A. 현재 프로젝트 상태 (실측)

작업 폴더를 전부 스캔한 결과입니다. **빈 프로젝트가 아닙니다.**

| 항목 | 상태 | 비고 |
|---|---|---|
| `MOOKI(General-purpose Discord server bot)/` | **가동 이력 있음** | discord.js v14, 912줄 단일 파일. §4.1의 "이미 보유한 범용 봇" |
| └ 보안 모듈 | 구현됨 | 스팸/피싱링크/대량멘션/레이드(대량입장) 탐지 |
| └ 티켓 시스템 | 구현됨 | 문의/신고/건의 3종 — **§3.8 신고 시스템의 기반** |
| └ 반응 역할, 임베드 빌더, 채널설정 | 구현됨 | 유지 대상 |
| └ 경제 미니게임 | 구현됨 | 가입/출석/주식/창업/상장/인수/랭크/프로필 — **§4.1 제거 대상** |
| └ `.env` | **RCON 정보 이미 존재** | `MC_HOST` / `MC_RCON_PORT` / `MC_RCON_PW` 채워져 있음 → 어딘가에 MC 서버가 이미 있음(확인 필요) |
| └ `bot.log` | **크래시 상태** | `messageCreate` 핸들러에서 unhandled error → 프로세스 종료. 로그 경로도 구 위치(`Desktop\code\discord_bot\MOOKIbot`) |
| `ruc_server_icon/` | 준비됨 | `non_bacground.png`(투명배경 800×800), `common.png` |
| `web_background/` | 준비됨 | candidate_1/2/3.png (2559×1439 셰이더 스크린샷) |
| `conversation.md` | 0바이트 | 빈 파일 |
| 마인크래프트 서버 파일 | **없음** | 전부 신규 구축 |
| 웹사이트 | **없음** | 전부 신규 구축 |
| git 저장소 | **아님** | Vercel 연동 위해 §7.7에서 초기화 필요 |

### 즉시 발견된 문제 3가지

1. **MOOKI가 지금 죽어 있습니다.** `bot.log` 마지막 기록이 unhandled `error` 이벤트로 인한 프로세스 종료입니다. `client.on('error')` 핸들러와 최상위 예외 처리가 없습니다.
2. **`.env`가 평문으로 폴더에 있습니다.** 디스코드 토큰과 RCON 비밀번호가 그대로 들어 있고, Vercel 연동을 위해 git을 붙이는 순간 유출 위험이 생깁니다. → Phase 0에서 `.gitignore` 먼저, 토큰 재발급 권장.
3. **RUC 이름 충돌.** MOOKI 경제 미니게임의 화폐 이름이 이미 "RUC"이고 `/ruc지급` 명령까지 있습니다. 그런데 §3.1의 Ruc는 **마인크래프트 4개 서버 공용 화폐**입니다. 디스코드 주식게임 잔고와 인게임 Ruc가 같은 이름의 다른 화폐면 유저가 반드시 혼동합니다. → 결정 필요 (`02-OPEN-QUESTIONS.md` Q4)

---

## B. 아키텍처 결론

```
                        ┌─────────────────────────┐
   플레이어 ──접속──▶   │  Velocity 프록시          │  ← 단일 접속 주소
                        │  (로비 강제 진입)         │
                        └───────────┬─────────────┘
                    ┌───────────┬───┴───┬───────────────┐
                    ▼           ▼       ▼               ▼
                ┌───────┐  ┌───────┐ ┌───────┐  ┌─────────────┐
                │ HOME  │  │ RAID  │ │ WAR   │  │ PEACE       │
                │ 우선0 │  │ 우선1 │ │ 우선2 │  │ 우선3       │
                │ PvP X │  │ 콤로그│ │ 국가  │  │ 간접살해 X  │
                │ 건축 X│  │ 처형  │ │ 코어  │  │             │
                └───┬───┘  └───┬───┘ └───┬───┘  └──────┬──────┘
                    └──────────┴─────────┴─────────────┘
                                   │
                        ┌──────────▼────────────┐
                        │  RucCore (공용 플러그인) │
                        │  Ruc 화폐 · 자체 XP     │
                        │  평판 · 길드 · 스코어보드 │
                        └──────────┬────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │ MySQL/MariaDB (네트워크 공유) │
                    └──────────────┬──────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        ▼                          ▼                          ▼
  ┌───────────┐            ┌──────────────┐           ┌───────────────┐
  │ MOOKI 봇  │            │ 채팅 중계 봇  │           │ 웹사이트       │
  │ 관리/티켓 │            │ 웹훅+스킨헤드 │           │ Next.js→Vercel │
  └───────────┘            └──────────────┘           └───────────────┘
```

**핵심 설계 원칙:** 4개 서버가 각자 플러그인을 갖는 구조가 아니라, **`RucCore` 하나가 공용 시스템(화폐·XP·평판·길드·스코어보드)을 전부 소유**하고 서버별 모듈(`RucHome`, `RucRaid`, `RucWar`, `RucPeace`)이 그 위에 규칙만 얹습니다. Ruc가 "4개 서버 공용"이라는 요구사항(§3.1)과 드래곤 알 버프가 "어느 서버에 있든 네트워크 전역 적용"이라는 요구사항이 **단일 DB 소스를 강제**하기 때문입니다.

---

## C. 단계별 개발 계획

각 Phase 끝에서 **체크인**합니다. Phase는 §2.1 우선순위(홈 → 레이드 → 국가전 → 평화)를 따르되, 웹사이트는 마크 호스팅 결정을 기다릴 필요가 없으므로 병렬로 진행합니다.

---

### Phase 0 — 기반 정리 · 보안 (0.5일)

**호스팅 결정과 무관하게 지금 바로 가능.**

- [ ] `git init` + `.gitignore` (`.env`, `node_modules/`, `*.log`, 월드 데이터, 플러그인 jar)
- [ ] `.env` → `.env.example` 템플릿 분리, 실제 `.env`는 추적 제외
- [ ] **디스코드 봇 토큰 재발급** (평문 노출 이력) + RCON 비밀번호 변경 권장
- [ ] MOOKI 크래시 수정: `client.on('error')`, `process.on('unhandledRejection')`, 핸들러별 try-catch
- [ ] MOOKI를 `discord/mooki/`로 이동, 경로 정리
- [ ] 저장소 구조 확정 (`docs/`, `web/`, `minecraft/`, `discord/`)

**완료 조건:** MOOKI가 죽지 않고 뜬다. git에 비밀정보가 없다.

---

### Phase 1 — 웹사이트 (2~3일) · 병렬 진행

§7 전체. 호스팅 결정을 기다리지 않으므로 **가장 먼저 눈에 보이는 결과물**이 나옵니다.

- [ ] Next.js(App Router) + Tailwind, `web/`에 스캐폴딩
- [ ] 디자인 토큰: `#bad953` / `#93e93e` + 글래스모피즘 유틸리티
- [ ] 픽셀 폰트: **Galmuri**(한글 픽셀 폰트, OFL) + **Silkscreen / Pixelify Sans**(영문). 한글 지원되는 픽셀 폰트가 필수라 Galmuri가 사실상 유일한 현실적 선택
- [ ] 배경: **candidate_2.png 채택** (선정 근거 아래) → blur + 어두운 오버레이
- [ ] 상단 네비(§7.3): 반투명 프로스티드, 좌측 아이콘 + "Ruc Server", 우측 KO/EN 토글 → "서버 가기" 디스코드 버튼
- [ ] i18n: 한국어 기본 / 영어 (`/` = ko, `/en` = en)
- [ ] 랜딩 페이지(§7.2): 히어로 → 문제·해결 서사 → 해결하는 기능 연결 → 추가 섹션(D8)
- [ ] 홈페이지(§7.3): **실시간 서버 상태 대시보드** — 4개 모드별 온라인 여부/인원/TPS, 공지, 빠른 링크
- [ ] 서버 상태 API: `/api/status` — 마크 서버 핑(`minecraft-server-util`) + 30초 캐시
- [ ] URL 규칙 확정(§7.8) + Vercel 배포

**완료 조건:** `ruc-server.vercel.app`이 살아 있고, 서버 상태가 실시간으로 표시된다.

**배경 선정 근거:** candidate_2 채택.
- candidate_1 — 구름이 화면 상단을 꽉 채워 네비바 뒤가 밝은 회색. 프로스티드 글래스 네비의 대비가 죽습니다.
- candidate_3 — 석양 주황 톤이라 브랜드 그린(`#93e93e`)과 보색 충돌. 픽셀 폰트 흰 텍스트 가독성도 가장 나쁩니다.
- candidate_2 — 상단이 진한 청색(네비바 대비 확보) + 하단이 채도 높은 잔디 녹색(브랜드 컬러로 자연스럽게 이어짐). 셋 중 유일하게 브랜드 팔레트와 같은 계열입니다.

---

### Phase 2 — 마크 네트워크 골격 + 홈 서버 (§2.1 우선순위 0) (3~4일)

**호스팅 결정 필요** → `02-OPEN-QUESTIONS.md` Q1

- [ ] Paper + Velocity 설치, 버전 확정 (Q2)
- [ ] Velocity 설정: 모던 포워딩, 로비 강제 진입, 서버 4개 등록
- [ ] MySQL/MariaDB 준비 (공용 데이터)
- [ ] **홈 서버 구축**
  - [ ] 자유 이용 라이선스 광장 맵 확보 + 라이선스 원문 보관
  - [ ] PvP 비활성, 블록 파괴/설치 차단
  - [ ] 어드벤처 모드, 낙하 데미지 없음, 항상 낮/맑음, 배고픔 고정
  - [ ] NPC 서버 선택기 · 정보 홀로그램 · 나침반 GUI (D1)
- [ ] **전역 규칙**: 경험치바 위 좌표 표시 억제 (§2.2)
- [ ] **RucCore v0**: Ruc 화폐, 자체 XP, 스코어보드 골격
- [ ] 공용 명령어: `/tpa` `/tpahere` `/tpdeny` `/tpcancel`
- [ ] 한국어/영어 언어 파일 (§6.3)

**완료 조건:** 접속하면 홈 광장에 떨어지고, NPC로 다른 서버(빈 월드라도)로 이동되고, 우측 스코어보드가 뜬다.

---

### Phase 3 — 레이드 서버 (우선순위 1) (2~3일)

- [ ] `RucRaid`: **전투 중 접속 종료 → 재접속 시 강제 사망 + 아이템 전부 소멸 + XP 전부 소멸** (§2.4)
  - 전투 태그 15초. 태그 상태에서 종료 시 DB에 `pending_execution` 플래그
  - 재접속 시 인벤토리 `clear()` **후** `setHealth(0)` — 순서가 중요. **드랍이 남지 않는 것이 핵심**(콤벳로그로 아이템을 지인에게 넘기는 루트를 차단)
  - 자체 XP도 0으로 초기화
- [ ] `/spawn` `/sethome` `/home` + 한글 별칭 `/스폰` `/셋홈` `/홈`
- [ ] 드래곤 알 1개 배치 + 버프 (D3)
- [ ] 에픽/보스 몬스터 XP 소스

---

### Phase 4 — 국가전 서버 (우선순위 2) (5~7일) — 최대 난이도

- [ ] 길드 시스템(§3.5): 길드장/부길드장/길드원, 창설 조건(최소 레벨 + Ruc), 주간 보상, 시즌 랭킹 보상
- [ ] **국가 승격**: 길드원 수 임계치 초과 시 Nation 인정
- [ ] **입장 제한**: 국가 소속이 아니면 서버 진입 차단 (Velocity 단에서 거부 — 서버에 들어온 뒤 킥하면 로딩 낭비)
- [ ] **코어 제작 체인** (D2) + 코어 아이템
- [ ] **영토 시스템**: 코어 설치 지점 → 청크 클레임, 전쟁 시간대에만 탈취 가능
- [ ] **전쟁 시간대 스케줄러**: 피크 타임에만 개전, 그 외엔 자원 채집/강화만
- [ ] `/storage`(`/국가창고`), `/tp [코어명]`
- [ ] 전쟁 중 무기 제한 없음 / 평시 영토 규칙

---

### Phase 5 — 평화 서버 (우선순위 3) (2일)

- [ ] `RucPeace`: 직접 PvP 차단 **+ 간접 살해 전부 차단** (§2.5)
  - 엔드 크리스탈, TNT 마인카트, 베드/리스폰 앵커 폭발, 낙하 유도, 용암/물 밀치기 등
  - 구현 방식: 플레이어→플레이어 데미지의 **근본 원인(root damager)을 역추적**해 전부 취소. 발사체·폭발·환경 데미지까지 체인을 따라감
- [ ] 테러/그리핑 탐지 로그 → Helper 재량 제재 도구
- [ ] 드래곤 알 1개 배치
- [ ] `/spawn` `/sethome` `/home`

---

### Phase 6 — 디스코드 (3~4일)

- [ ] **MOOKI 정리**(§4.1): 경제 미니게임 제거, 관리 기능 추가 (D6)
- [ ] **채팅 중계 봇**: 서버별 채널 매핑, **플레이어 스킨 헤드 웹훅**으로 발언 렌더링, 시스템 알림(입퇴장/발전과제)은 임베드로 분리
- [ ] **티켓 봇 → 신고 폼**(§3.8): 가해자 닉네임 / 사유 / 발생시각 `hh:mm:ss`
- [ ] 인게임 `/report`(`/신고`) → 디스코드 신고 채널 연결
- [ ] 스태프 역할 동기화(§3.2), 평판 티어 = 디스코드 역할(§3.7)
- [ ] 제재 티어 자동화(§3.9 + D5): 디스코드 뮤트 + RCON 마크 밴 동시 집행

---

### Phase 7 — 평판 · 신고 · 제재 통합 (2일)

- [ ] 평판 7티어 (초록→노랑→빨강→보라→파랑→남색→검정)
- [ ] 하락: 피신고 확정 / 공식 제재
- [ ] 상승: 2가지 방법 (D4)
- [ ] 제재 티어 1~10 (D5)

---

### Phase 8 — 수익화 (2~3일)

- [ ] 후원 등급 7단계 혜택 구현 (D7) — **PvP 관련 요소는 절대 판매 금지**
- [ ] Ruc 충전 / 코스메틱 상점
- [ ] 디스코드 부스트 티어 (Hyper / Rocket / Booster)
- [ ] 결제 연동 → Q5

---

### Phase 9 — 폴리싱 · 런칭 (상시)

- [ ] 부하 테스트, 안티치트, 백업 자동화
- [ ] 리소스팩 / UI (Designer 역할)
- [ ] 웹사이트 신규 페이지 확장 (§7.8 URL 규칙 준수)

---

## D. 일정 요약

| Phase | 내용 | 기간 | 차단 요소 |
|---|---|---|---|
| 0 | 기반·보안 | 0.5일 | ✅ **완료** |
| 1 | 웹사이트 | 2~3일 | ✅ **1차 완료** (배포만 남음) |
| 2 | 네트워크 + 홈 | 3~4일 | JDK 21 설치 확인 (Q3) |
| 3 | 레이드 | 2~3일 | Phase 2 |
| 4 | 국가전 | 5~7일 | Phase 2 |
| 5 | 평화 | 2일 | Phase 2 |
| 6 | 디스코드 | 3~4일 | Phase 2 (RCON) |
| 7 | 평판·제재 | 2일 | Phase 6 |
| 8 | 수익화 | 2~3일 | Q5 |
| 9 | 폴리싱 | 상시 | — |

**Phase 0 + 1은 지금 바로 착수 가능합니다.** Phase 2부터는 호스팅 답이 필요합니다.

---

# 진행 기록

## ✅ Phase 0 — 기반 · 보안 (완료, 2026-09-12)

| 작업 | 결과 |
|---|---|
| `git init` + `.gitignore` | 완료. `.env`·`node_modules`·월드데이터·빌드산출물 제외 |
| 비밀정보 차단 검증 | `git add -An` 드라이런으로 확인 — `.env`는 제외, `.env.example`만 포함 |
| `.env.example` 템플릿 | MOOKI용 + 웹용 두 개 생성 |
| **MOOKI 크래시 원인 규명** | `client.on('error')` 리스너 부재. discord.js v14는 async 리스너가 reject하면 그 오류를 client의 `'error'` 이벤트로 재발행하는데, 리스너가 없으면 Node가 프로세스를 죽입니다 |
| MOOKI 크래시 수정 | `client.on('error')`, `shardError`, `unhandledRejection`, `uncaughtException` 핸들러 추가 + `safeListener()` 래퍼로 모든 이벤트 리스너 감쌈 |
| 수정 검증 | 잘못된 토큰으로 기동 → 예전이면 프로세스 종료. 지금은 `[unhandledRejection]`으로 로그만 남기고 **생존 확인** |
| `economy_data.json` 백업 | `docs/backup/economy_data.backup.json` |

### ⚠️ 소유자가 직접 해야 하는 일

1. **디스코드 봇 토큰 재발급** — `.env`에 평문으로 있었고 이력이 남습니다. Discord Developer Portal에서 Reset Token
2. **RCON 비밀번호 변경** — 현재 5자입니다. 너무 짧습니다

## ✅ Phase 1 — 웹사이트 (1차 완료, 2026-09-12)

Next.js 15 (App Router) + Tailwind v4. `web/` 폴더. **빌드 통과, 전 경로 200 확인, 브라우저 실물 확인 완료.**

| 항목 | 결과 |
|---|---|
| 랜딩 페이지 (§7.2 + D8) | 11개 섹션 전부 구현. 히어로 → 상태 스트립 → 문제·해결·기능 → 4모드 → 국가전 타임라인 → 드래곤 알 → 평판 → 후원 → 로드맵 → FAQ → 최종 CTA |
| 디스코드 CTA (§7.2 필수) | 3곳 배치 — 네비 고정 / 히어로 직하 / 최종 섹션 |
| 홈페이지 = 실시간 상태 (§7.3) | `/status` — 4개 모드별 온라인·인원·핑·버전, 전체 접속자, 공지, 바로가기 |
| 상단 네비 (§7.3) | 프로스티드 글래스. 좌: 아이콘+"Ruc Server" / 우: KO·EN 토글 → "서버 가기" |
| 언어 전환 | 경로 유지 확인 (`/en/status` ↔ `/status`) |
| 픽셀 폰트 (§7.5) | **Galmuri11** 자체 호스팅. 한글 지원 픽셀 폰트라 선택 (영문 전용 폰트는 한글이 깨짐). OFL 전문 동봉 |
| 글래스모피즘 (§7.5) | `.glass` / `.glass-strong` / `.glass-nav` |
| 브랜드 컬러 (§7.1) | `#bad953` / `#93e93e` 기반 9단계 스케일 |
| 배경 (§7.6) | **candidate_2 채택** → 1600px JPEG 압축 (4.59MB → **126KB**) → CSS blur |
| 한국어/영어 (§7.4) | 전 문안 ko/en 쌍. `lib/content.ts` 한 곳에서 관리, 타입으로 누락 방지 |
| 서버 상태 API | `/api/status` — mcstatus.io, 30초 캐시. 주소 미설정 시 "준비 중" 표시하고 사이트는 정상 동작 |

### 설계 판단 2가지 (명세와 다르게 간 부분)

1. **서버 핑을 raw TCP가 아니라 HTTP API로.** `minecraft-server-util`은 현재 유지보수 중단 상태이고, Vercel 서버리스에서 임의 포트 TCP 소켓은 콜드스타트마다 타임아웃이 납니다. mcstatus.io 공개 API가 확실합니다.
2. **`@import`가 아니라 폰트 자체 호스팅.** 처음 시도한 Galmuri CDN 주소 3개가 전부 404였습니다. npm 패키지(`galmuri`)에서 woff2를 꺼내 `public/fonts/`에 넣었습니다. 외부 의존성이 없어지고 로딩도 빠릅니다.

### 남은 작업 (소유자 필요)

- [ ] GitHub 저장소 생성 + 푸시
- [ ] Vercel 연결 — **Root Directory를 `web`으로 지정해야 합니다** (저장소 루트 아님)
- [ ] 배포 후 실제 도메인 확인

배포 절차는 `web/README.md`에 정리해 두었습니다.
